{
  "name": "little-tasks",
  "version": "0.1.0",
  "description": "Little Tasks — a simple tasks tray app with local memory and cross-device portability.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "dev:desktop": "tauri dev",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "tauri": "tauri",
    "check": "svelte-check"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.4.0",
    "@tauri-apps/plugin-autostart": "^2.5.1",
    "@tauri-apps/plugin-dialog": "^2.7.2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tauri-apps/cli": "^2.4.0",
    "svelte": "^5.20.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
